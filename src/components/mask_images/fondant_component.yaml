name: Mask images
description: Component that masks images based on bounding boxes
image: ghcr.io/ml6team/mask_images:0.8dev5

consumes:
  image:
    type: binary
  face_bboxes:
    type: array
    items:
      type: array
      items:
        type: int32

produces:
  image:
    type: binary