name: Mask images
description: Component that masks images based on bounding boxes
image: fndnt/mask_images:0.8.0

consumes:
  image:
    type: binary
  face_bboxes:
    type: array
    items:
      type: array
      items:
        type: int32

produces:
  image:
    type: binary