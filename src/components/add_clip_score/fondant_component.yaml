name: Add CLIP score
description: Component that adds the CLIP score
image: fndnt/add_clip_score:0.8.0

consumes:
  embedding:
    type: array
    items:
      type: float32
  text_embedding:
    type: array
    items:
      type: float64

produces:
  image_text_clipl14score:
    type: float64